# Beli - Personal AI Agent
# Install with: pip install -r requirements.txt

# Beli's brain (Claude AI)
anthropic>=0.25.0

# Telegram channel (job-queue required for scheduled tasks)
python-telegram-bot[job-queue]>=20.7

# Telethon: Telegram User API (to send messages to the owner's contacts)
telethon>=1.36.0

# HTTP requests (for AgentMail API)
requests>=2.31.0

# Environment variables
python-dotenv>=1.0.0

# Async database (persistent memory)
aiosqlite>=0.20.0

# Groq (voice note transcription via Whisper)
groq>=0.9.0

# Timezone database for zoneinfo (required on Linux/Railway)
tzdata>=2024.1

# Google Calendar API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# OpenAI (GPT-4o for general conversation without tools)
openai>=1.30.0

# X / Twitter API v2
tweepy>=4.14.0

# Firecrawl (web browsing and search)
firecrawl-py>=1.0.0
