# Variables de entorno (NUNCA subir a git)
.env

# Owner's personal profile (contains private information)
owner-profile.md
*-profile.md

# Data folder — all generated/sensitive files
data/*.db
data/*.session
data/contact_cache.json
data/beli_session.session
data/google_calendar_credentials.json
data/gmail_credentials.json

# Google OAuth client secret (never commit)
google_client_secret.json

# Personal files — use the .example versions as templates
contacts.json
reminders.md

# Logs
logs/*.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/

# macOS
.DS_Store
